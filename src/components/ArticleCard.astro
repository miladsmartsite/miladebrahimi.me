---
import type { CollectionEntry } from 'astro:content';
import PlaceholderTag from '@components/PlaceholderTag.astro';

interface Props {
  article: CollectionEntry<'articles'>;
}

const { article } = Astro.props;
const { title, description, publishDate, tags, placeholder } = article.data;
const href = article.data.externalUrl ?? `/articles/${article.id}`;
const formattedDate = publishDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<a href={href} class="group/row block border-b border-ink-100 py-7 transition-colors first:border-t first:pt-0">
  <div class="flex flex-wrap items-center gap-3">
    <time datetime={publishDate.toISOString()} class="text-xs text-ink-500">{formattedDate}</time>
    {placeholder && <PlaceholderTag />}
  </div>
  <h3 class="mt-2 font-display text-xl text-ink-950 transition-colors group-hover/row:text-accent-ink sm:text-2xl">
    {title}
  </h3>
  <p class="mt-2 max-w-2xl text-sm text-ink-700">{description}</p>
  {tags.length > 0 && (
    <ul class="mt-3 flex flex-wrap gap-x-3 gap-y-1">
      {tags.map((tag) => (
        <li class="text-xs tracking-wide text-ink-500 uppercase">{tag}</li>
      ))}
    </ul>
  )}
</a>
