---
import type { CollectionEntry } from 'astro:content';
import Section from '@components/Section.astro';
import PlaceholderTag from '@components/PlaceholderTag.astro';
import { HOME } from '@lib/site';

interface Props {
  talks: CollectionEntry<'speaking'>[];
}

const { talks } = Astro.props;
---

<Section kicker={HOME.speaking.kicker} heading={HOME.speaking.heading} tone="dim" reveal>
  <ol class="border-t border-line">
    {talks.map((talk) => (
      <li class="group flex flex-col gap-2 border-b border-line py-6 sm:flex-row sm:items-baseline sm:gap-8">
        <span class="font-mono text-[11px] text-meta uppercase tracking-wide sm:w-32 sm:shrink-0">
          {talk.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long' })}
        </span>
        <div class="flex-1">
          <div class="flex flex-wrap items-center gap-3">
            <h3 class="font-display text-xl font-bold tracking-[-0.02em] text-ink transition-colors group-hover:text-accent-ink sm:text-2xl">
              {talk.data.title}
            </h3>
            {talk.data.placeholder && <PlaceholderTag />}
          </div>
          <p class="mt-1 font-body text-base text-body">
            {talk.data.event}
            {talk.data.location && <span class="text-meta"> · {talk.data.location}</span>}
          </p>
        </div>
      </li>
    ))}
  </ol>
</Section>
