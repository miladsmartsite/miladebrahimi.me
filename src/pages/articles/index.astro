---
// The articles *listing* now lives at /contents/articles (Phase 3's
// Contents architecture). Individual article pages stay exactly where
// they were — only this index moved, and it's kept as a redirect so old
// links/bookmarks to /articles keep working.
return Astro.redirect('/contents/articles', 301);
---
