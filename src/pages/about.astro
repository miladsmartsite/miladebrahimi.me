---
// About's content merged into /resume's "Profile" section (Phase 3 §14 —
// the nav item is now "About Me & Resume", a single destination). Kept as
// a redirect, not a deleted route, so old links/bookmarks to /about keep
// working.
return Astro.redirect('/resume/', 301);
---
