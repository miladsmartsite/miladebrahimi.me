---
// /work (the older role-by-role experience timeline) is now redundant
// with /resume's Career section (same Timeline component, same
// `experience` collection) — and its name collided with the nav's
// canonical "Work" destination, /projects (the systems/case-study
// archive). Per Phase 6 §07, /projects is the one Work destination.
// Kept as a redirect, not a deleted route, so old links/bookmarks to
// /work keep working.
return Astro.redirect('/projects', 301);
---
