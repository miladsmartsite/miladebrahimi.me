---
// The books listing now lives at /contents/books (Phase 3's Contents
// architecture). Kept as a redirect, not a deleted route, so old
// links/bookmarks to /books keep working.
return Astro.redirect('/contents/books', 301);
---
